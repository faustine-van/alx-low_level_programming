#include <stdio.h>
/**
 *main -  prints its name
 *@argc: count number of argument not used
 *@argv: an array of size of argc
 *Return: 0;
*/
int main(int __attribute__((unused)) argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
