#include <stdio.h>
#include <ctype.h>
#include <main.h>
/**
 *main -entry
 *
 *Return: 0
*/

/**
 * _islower() -
 *
 *Return: 0
*/
int _islower(char c;)
{
	int j;
	char c;

	if (islower(j))
	{
		j = c;
	} else
	{
		printf(" is not lower case");
	}
	return (j);
}

int main(void)
{
	int r = _islower('j');

	putchar(r + '0');
	return (0);
}
